# SQL Practice

This repository contains my SQL learning journey, practice queries, and exercises covering the core SQL concepts required for **Data Analysis and Data Science**.

## 📚 Topics Covered

### 1. SQL Fundamentals

* `SELECT`
* `WHERE`
* `DISTINCT`
* `ORDER BY`
* Aliases
* SQL comments

### 2. SQL Data Types & Constraints

* `INT`
* `VARCHAR`
* `DECIMAL`
* `DATE`
* `DATETIME`
* `PRIMARY KEY`
* `FOREIGN KEY`
* `UNIQUE`
* `NOT NULL`
* `DEFAULT`
* `CHECK`

### 3. DDL — Data Definition Language

* `CREATE`
* `ALTER`
* `DROP`
* `TRUNCATE`

### 4. DML — Data Manipulation Language

* `INSERT`
* `SELECT`
* `UPDATE`
* `DELETE`

### 5. Aggregate Functions

* `COUNT()`
* `SUM()`
* `AVG()`
* `MIN()`
* `MAX()`

### 6. GROUP BY & HAVING

* Grouping data
* Aggregating grouped data
* Filtering grouped results using `HAVING`

### 7. Joins
* `CROSS JOIN (CARTESIAN PDODUCT)`
* `INNER JOIN`
* `LEFT JOIN`
* `RIGHT JOIN`
* Self Join
* Joining multiple tables

### 8. Subqueries

* Scalar subqueries
* Subqueries with `WHERE`
* Subqueries with `FROM`
* Subqueries with `SELECT`
* Subqueries with `INSERT`
* Subqueries with `UPDATE`
* Subqueries with `DELETE`

### 9. Window Functions

* `ROW_NUMBER()`
* `RANK()`
* `DENSE_RANK()`
* `LAG()`
* `LEAD()`
* `SUM() OVER()`
* `AVG() OVER()`
* Window `PARTITION BY`
* Window `ORDER BY`
* Window frames
* `CUME_DIST()`

### 10. Set Operations

* `UNION`
* `UNION ALL`
* `INTERSET`
* `EXCEPT(MINUS)` 

### 12. Common SQL Functions

* String functions
* Date and time functions
* Numeric functions

### 13. CTEs 

* Common Table Expressions (`WITH`)


---

## 🧠 What I Practiced

Through this repository, I practiced solving SQL problems involving:

* Data filtering
* Sorting and ranking
* Aggregation
* Group-wise analysis
* Multiple-table analysis
* Nested queries
* Running totals
* Rankings
* Previous/next row analysis
* Cumulative calculations
* Business-oriented analytical questions

---

## 🛠️ Tools Used

* **MySQL**
* **MySQL Workbench**

---
**Keep Learning. Keep Building. 🚀**
